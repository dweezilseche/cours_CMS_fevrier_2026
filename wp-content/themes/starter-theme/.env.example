# Configuration de l'environnement local
# Copier ce fichier en .env et ajuster les valeurs.
# Ne jamais committer le fichier .env ni y mettre de vrais identifiants.

# WordPress (à définir aussi dans wp-config.php pour être pris en compte)
WP_ENVIRONMENT_TYPE=local
WP_DEBUG=true
WP_DEBUG_LOG=true
WP_DEBUG_DISPLAY=false

# Base de données — placeholders (remplacer par vos valeurs dans .env / wp-config.php)
DB_NAME=wp_db
DB_HOST=localhost
DB_USER=wp_user
DB_PASSWORD=votre_mot_de_passe

# URLs
SITE_URL=http://votre-site.local
VITE_DEV_SERVER=http://localhost:5173
