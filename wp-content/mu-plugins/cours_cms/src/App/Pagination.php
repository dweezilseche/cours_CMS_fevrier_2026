<?php

namespace App;

defined('ABSPATH') || exit;

/**
 * Pagination globale (optionnel : filtres Twig, helpers).
 */
class Pagination
{
    public static function init(): void
    {
        // Hooks éventuels pour la pagination (ex: timber/twig pour une fonction pagination())
    }
}
